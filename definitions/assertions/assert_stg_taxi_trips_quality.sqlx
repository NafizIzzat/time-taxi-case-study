config {
  type: "assertion",
  schema: "dataform_assertions",
  name: "assert_stg_taxi_trips_has_rows",
  description: "Fails if the staging table is empty."
}

SELECT
  row_count
FROM (
  SELECT
    COUNT(*) AS row_count
  FROM ${ref("stg_taxi_trips")}
)
WHERE row_count = 0