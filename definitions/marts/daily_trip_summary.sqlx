config {
  type: "view",
  schema: "us_taxi_trips",
  name: "daily_trip_summary",
  description: "Dashboard-facing view of the reusable daily trip aggregate."
}

SELECT
  *
FROM ${ref("agg_daily_trips")}